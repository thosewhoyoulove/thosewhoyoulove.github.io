# 不确定性下的前端状态机

## 面试定位

JD 强调：流式、非结构化、不确定数据下仍要 **流畅且确定的体验**。面试官想听你有没有把 Agent 运行建模成 **可推导的状态机**，而不是一堆布尔值和临时 `useState`。

## 面试回答

可以这样答：

> 我会把一次 Agent 执行建模成 Run，而不是堆叠 `isLoading`。服务端产生带 `runId、eventId、seq、type` 的事件，前端只通过 reducer 归约出视图状态；重复事件按 `eventId` 去重，序号断档触发补拉，旧 Run 的迟到事件直接丢弃。Run 本身有 running、awaiting_user、succeeded、failed、cancelled 等状态，文本、工具和 UI block 再维护各自的子状态。不确定字段显式区分 unknown、partial 和 final，只有经过校验的 final 数据才能驱动不可逆操作。断线恢复时优先用“服务端快照 + 游标后的增量事件”重建，而不是相信浏览器内存。这样模型输出可以随机，但状态迁移、恢复和副作用边界仍然确定、可测、可回放。

一句话总结：

> 不确定的是模型内容；确定的是 Run 状态机与事件归约。

---

## 核心原理

```text
用户意图 / 主动推送
  → 创建 Run
  → 事件流：text / ui_block / tool / ask_user / error / done
  → reducer(run, event) → nextRun
  → 视图按 run.status 与 blocks 渲染
```

```ts
type RunStatus =
  | 'idle'
  | 'running'
  | 'awaiting_user'
  | 'succeeded'
  | 'failed'
  | 'cancelled'

type Field<T> =
  | { kind: 'unknown' }
  | { kind: 'partial'; value: T }
  | { kind: 'final'; value: T }

type RunEvent = {
  runId: string
  eventId: string
  seq: number
  type: string
  itemId?: string
  payload: unknown
  createdAt: string
}
```

---

## 1. 为什么要用状态机

| 没有状态机 | 有状态机 |
| --- | --- |
| 多个 `isLoading` 互相打架 | 同一时刻一个主状态 |
| 取消后仍写入旧流 | 事件带 runId，过期事件丢弃 |
| 无法复现 bug | 事件日志可回放 |
| 不确定字段用 `any`/`null` 混用 | `unknown/partial/final` 语义清晰 |

**要点**：先画状态迁移图，再写组件。

---

## 2. 关键迁移与不变式

典型迁移：

```text
idle → running（用户发送或 Agent 主动发起）
running → awaiting_user（HITL / 澄清问题）
awaiting_user → running（用户提交）
running → succeeded | failed | cancelled
```

不变式（面试可背）：

1. 非 `running/awaiting_user` 时不接受该 run 的 delta（或仅接受迟到的 `done` 校正）。
2. `cancelled` 后本地 buffer 冻结，新事件若 runId 匹配也丢弃（除非服务端声明 resume）。
3. `partial` 不得驱动不可逆业务写（下单、打款）；只有 `final` + 用户确认可以。
4. 相同 `eventId` 只能生效一次；发现 `seq` 断档时暂停归约并补拉，不能猜测缺失内容。

服务端的 Run 快照和事件日志是事实来源；React/Zustand 中的 buffer 只是可丢弃的投影。恢复时先取 `snapshot(seq=120)`，再消费 `seq>120` 的事件，既避免从头回放，也避免断线期间丢包。

---

## 3. 与流式、生成式 UI 的衔接

- 文本：`partial` 字符串 append，`final` 可再 normalize。
- UI block：同一 `blockId` patch；`status` 从 partial → ready。
- 工具：独立子状态机挂在 run 下，见工具展示文。

**场景（示例）**：商家验证 Agent 先流式给出“疑似风险点”partial 列表，校验完再变 final；在 partial 阶段“一键冻结店铺”按钮禁用，避免基于半成品决策。

**验证**：属性测试式用例（乱序事件、重复 seq、取消后迟到包）；Storybook 回放失败 run。

---

## 高频追问

### 乐观更新还能做吗？

能，但要绑定 `expectedSeq` 或临时 clientId；服务端权威事件到达后 reconcile，冲突时回滚并提示。

### 多会话怎么隔离？

每个 session 独立 run 队列与 AbortController；切会话不共享 reducer 状态。

### 和 Redux/Zustand 什么关系？

状态机是领域模型；Redux/Zustand 只是存放 run 快照的容器。关键是事件归约是否纯、是否可回放。

---

## 延伸阅读

- [生成式 UI 与动态组件协议](/md/Agent/生成式UI与动态组件协议.md)
- [对话界面架构](/md/Agent/对话界面架构.md)
- [流式渲染与 SSE](/md/Agent/流式渲染与SSE.md)
- [工具调用与结果展示](/md/Agent/工具调用与结果展示.md)
