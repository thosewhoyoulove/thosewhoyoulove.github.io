---
description: omen 面试知识库的写作、结构与站点维护底线（Docsify + 中文面试导向）
globs: docs/md/**/*.md,docs/README.md,docs/_sidebar.md,docs/scripts/generateSidebar.js
alwaysApply: false
---

# 知识库底线

本仓库是个人前端面试知识库（Docsify，部署 GitHub Pages）。写或改 `docs/` 下内容时遵守：

## 语言与受众

- 正文使用**简体中文**。
- 受众是**面试候选人**：打开文章先能背口述，再向下理解原理，追问时能落到机制 / 数据结构 / 源码。

## 优先级

技术准确性 > 面试可表达性 > 可理解性 > 原理深度 > 源码细节 > 篇幅。

## 文章结构（新文/大改必填）

原理深文默认：

1. `## 面试回答` — 标题后**第一个**章节；2～3 分钟口述稿（`> ` 引用块）
2. **一句话总结** — 关键词 + 流程箭头，唤起整条回答链
3. `## 核心原理` — 先 Why 与主链路，再机制；机制须可 Trace；解释关键变量「为何存在」
4. `## 常见误区` — 2～5 条（❌ / ✅ / 为什么）
5. `## 高频追问` — 4～8 条，按由浅到深追问链
6. `## 延伸阅读` — Docsify 站内路径

**不再生成** `面试定位`。

详细规范、模板与标杆：加载 skill `knowledge-article-author`。领域扩写、定稿校对、导航与复习网分别用 `fullstack-knowledge-expansion`、`agent-frontend-knowledge`、`technical-accuracy-review`、`docsify-knowledge-site`、`interview-curriculum-sync`。

## 质量

- 正文解释口述稿，禁止与口述无关的百科扩写；口述中的重要术语必须在正文有解释。
- 区分直觉模型 / 工程模型 / 源码模型；区分 Element、Fiber、DOM 与 Render / Commit。
- 每个核心结论至少追问一次「为什么」；写清设计取舍，不只描述「怎么做」。
- 不编造版本特性；不确定时标注需核实，或只写已验证行为。
- 代码块标明语言；示例宜短、可 Trace。
- 新增主题前先搜索 `docs/md/`，避免与深文/速记重复；速记页只保留提纲 + 链接深文。

## 站点

- 新文章路径：`docs/md/<分类>/标题.md`，文件名与现有风格一致。
- 新文或改导航后：在 `docs/` 目录执行 `pnpm run generate-sidebar`（或 `npm run generate-sidebar`）更新 `_sidebar.md`；新大类还需改 `docs/scripts/generateSidebar.js` 的 `SIDEBAR`。
- 详细流程：加载 skill `docsify-knowledge-site`。

## 安全

- 勿把密钥、内网地址、真实用户数据写入 markdown 或提交到仓库。
- 勿修改 `docs/index.html` 中的 Gitalk 等第三方凭证，除非用户明确要求。
